// import { expect } from "chai";
// import { login } from "../../main/core/auth";

describe("auth", () => {
  /*
  it("should be an instance of Error", () => {
    login()
      .then((obj: any) => {
        console.log("########## Got:");
        console.log(obj);
      })
      .catch((err: Error) => {
        console.log(err);
      });

    expect(true).to.not.throw;
  });
  */
});
